# Santis Domain Resolvers

Pure deterministic resolver functions for the frozen Santis OS Domain Standards v1.0.

This package contains no Airtable, network, UI, clock, filesystem, or persistence access. Evaluation time is supplied explicitly by callers.
