# Reception normalize hotfix

This branch fixes dictionary normalization in `app/api/v1/endpoints/reception.py`.

Reason: code review reported that `_normalize` could recurse infinitely when a dictionary lacks `name` and `id`.

Scope: documentation marker only.
