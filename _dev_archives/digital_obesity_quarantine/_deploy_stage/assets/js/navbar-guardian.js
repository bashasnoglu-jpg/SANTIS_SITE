/**
 * SANTIS NAVBAR GUARDIAN v1.0
 * DISABLED — caused infinite loop (setInterval + MutationObserver cycle)
 * Navbar is now stable; this aggressive protection is no longer needed.
 */
(function () {
    console.log("🛡️ [Guardian] Disabled (v1.1 — stability mode)");
})();
