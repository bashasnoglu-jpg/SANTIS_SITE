version: '3.8'

services:
  santis-sovereign:
    build: .
    container_name: santis-sovereign-gateway
    restart: always
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    networks:
      - sovereign-net

networks:
  sovereign-net:
    driver: bridge
