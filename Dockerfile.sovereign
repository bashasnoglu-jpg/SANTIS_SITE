# /santis-quantum-os/Dockerfile.sovereign
# Santis OS - Sovereign Edge & UI Container v1.0

FROM node:24.19.0-alpine
WORKDIR /santis-quantum-os
COPY package*.json pnpm-lock.yaml* ./
RUN npm install -g pnpm && pnpm install --ignore-scripts
COPY . .
CMD ["pnpm", "run", "dev:ui"]
